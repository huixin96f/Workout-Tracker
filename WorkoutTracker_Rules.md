# 训练历史追踪器 — 需求与规则说明书 (Handoff Spec)

本文件是 `workout-history.html` 的完整规格说明。配合该 HTML 文件一起导入 Claude Code 后，无需其他信息即可继续维护此项目。

---

## 1. 项目背景

用户（下称 Huixin）维护一个**四日哑铃训练循环**，用聊天方式逐次口头汇报每次训练的重量、组数、次数，由助手记录到一个**单文件 HTML artifact** 中。该 HTML 是一个自包含的训练历史可视化工具（图表 / 表格 / 日历三视图 + 趋势预测）。

- **所有重量单位为磅 (lb)。**
- 数据**硬编码**在 HTML 文件内的 `SESSIONS` 数组里（无后端、无 localStorage、无外部依赖，纯静态单文件）。
- 每次用户汇报新训练，就把新 session 追加进 `SESSIONS` 数组，更新 footer 日期区间，然后重新交付整个文件。
- **绝不编造数据**。只记录用户明确汇报的数字。措辞含糊时先向用户确认再记（例如"双手合拿"vs"每手 each"、日期、组次）。

---

## 2. 四日训练循环结构

助手根据**上一次记录的训练日**推断下一次该练哪天（循环：1→2→3→4→1…）。

| 训练日 | 主题 | 动作（默认顺序） |
|---|---|---|
| **第一天** | 推类 + 核心稳定 | ①哑铃平板卧推 ②坐姿哑铃推肩 ③高位下拉(宽握) ④绳索伐木* ⑤平板支撑 |
| **第二天** | 髋主导 + 下肢联动 | ①哑铃深蹲 ②罗马尼亚硬拉 ③哑铃箭步蹲 ④农夫行走 ⑤臀桥 |
| **第三天** | 拉类 + 单侧协调 | ①单臂哑铃划船 ②交替哑铃弯举* ③坐姿绳索划船 ④哑铃伐木* ⑤自行车卷腹* |
| **第四天** | 全身复合循环(无组间休) | ①哑铃深蹲推举 ②俯身哑铃划船 ③哑铃摇摆 ④简化波比跳 ⑤平板支撑* |

\* = 经用户调整过，非原始计划。详见第 4 节。

**原始计划的组数×次数**（来自最初的计划表图片）：
- 第一天：卧推 3×12｜推肩 3×10｜下拉 3×12｜(抗旋 3×每侧8，已被替换)｜平板支撑 3×40秒
  - **8/14 起：卧推和高位下拉一律改成每组 10 次**（见第 4 节）。
- 第二天：深蹲 3×12｜罗马尼亚硬拉 3×10｜箭步蹲 3×每侧8｜农夫行走 3×30秒(已改记步数)｜臀桥 3×15
  - **8/16 起：哑铃深蹲一律改成每组 10 次**（见第 4 节）。
  - **8/16 起：臀桥一律改成每组 10 次**（见第 4 节）。
- 第三天：单臂划船 3×每侧10｜绳索划船 3×12｜交替弯举 3×每侧10｜(侧平板 3×每侧30秒，已被替换)｜哑铃伐木 3×每侧10
- 第四天（3 轮循环，每轮间休 90 秒，动作间不休）：深蹲推举 12｜俯身划船 10｜哑铃摇摆 15｜简化波比跳 8｜(平板支撑转体 每侧8，已被替换)

---

## 3. 记录约定 (Recording Conventions)

这些约定决定数据如何写进 `sets` 以及 `unit` 字段：

1. **哑铃动作区分"each"（每只手）与"双手合拿"**。
   - `unit: "each"` = 每只手该重量（如深蹲 22.5 each = 两手各 22.5）。
   - `unit: "双手合拿"` = 两手共持一个重物。
   - 汇报措辞变化时**必须先确认**再记。
2. **哑铃摇摆、哑铃伐木**：始终按"双手合拿"记总重（不是 each）。
3. **绳索伐木 / 坐姿绳索划船 / 高位下拉**：器械配重，直接记配重数字（`unit: "配重"` 或 `"配重/侧"`）。
4. **农夫行走**：改为**记步数**（不记时间）。`sets` 里第二个数字是步数，如 `[35,60]` = 每手35lb走60步。`unit: "each·步"`。
5. **臀桥**：记录机器基座重量 + 加片。用户有时报总数（基座50+加片），有时只报加片重量——按用户当次所报如实记，note 里说明。`unit: "加片"`。
6. **自行车卷腹**：记每侧次数 + 仰卧起坐板档位（9档最陡）。`unit: "自重/侧"`，重量位为 0。
7. **平板支撑 / 侧平板**：计时，`sets` 第二个数字是秒数，重量位为 0，`unit: "秒"`。
8. **部分完成的训练**：session 加 `partial: true`；跳过的动作 `sets: []` 且加 `skipped: true`，note 注明"跳过"。
9. **易混动作名，不要合并**（8/13 用户核对过一次）：
   - **哑铃深蹲**（第二天）与**哑铃深蹲推举**（第四天）是两个动作，绝不可混记。
   - 现场分辨信号：深蹲一直在 **20–25 each**，深蹲推举一直在 **15–17.5 each**（后者最后要推过头顶，所以低一档）。
   - 同理 **哑铃伐木**（第三天，双手合拿）与 **绳索伐木**（第一天，器械配重）也是两个动作。

### 数据结构格式

```js
{ date: "YYYY-MM-DD", day: <1-4>, partial?: true, exercises: [
  { name: "动作名", unit: "each|双手合拿|配重|配重/侧|each/侧|each·步|加片|自重/侧|秒",
    sets: [[weight, reps], ...],   // 自重/计时动作 weight=0
    skipped?: true,                // 跳过时 sets: []
    note?: "备注" },
  ...
]},
```

- `sets` 是二维数组，每个子数组 `[重量, 次数或秒数或步数]`。
- 自重/计时动作重量填 `0`。

---

## 4. 动作替换与规则变更历史

导入后必须沿用这些既定变更（用户已明确决定）：

| 变更 | 详情 | 影响的训练日 |
|---|---|---|
| 哑铃抗旋 → 哑铃伐木 → **绳索伐木** | 第一天第4个动作。最初是哑铃抗旋，先改哑铃伐木，7/13 起再改绳索伐木（器械配重，`配重/侧`） | 第一天 |
| 侧平板支撑 → **自行车卷腹** | 第三天第4个动作。做在仰卧起坐板上，9档最陡 | 第三天 |
| 平板支撑转体 → **标准平板支撑** | 第四天第5个动作，改为计时 | 第四天 |
| 农夫行走：计时 → **记步数** | 见记录约定4 | 第二天 |
| 第三天动作顺序调整 | 两处：**交替哑铃弯举提到坐姿绳索划船之前**；**哑铃伐木在自行车卷腹之前**（8/14 用户确认，此前文档写反了，实际四场第三天都是伐木在前）。现行顺序：单臂划船 → 弯举 → 绳索划船 → **哑铃伐木 → 自行车卷腹** | 第三天 |
| 卧推 / 高位下拉 改 **每组 10 次** | 8/14 起，哑铃平板卧推与高位下拉的每一组都记 10 次，不再用 12 次 | 第一天 |
| **哑铃深蹲 改每组 10 次** | 8/16 起，第二天的哑铃深蹲每一组都记 10 次，不再用 12 次（覆盖"重的一组放中间"时的 12 次制） | 第二天 |
| **臀桥 改每组 10 次** | 8/16 起，第二天的臀桥每一组都记 10 次，不再用 15 次 | 第二天 |
| **哑铃伐木从第一天移走** | 8/14 起，第一天的趋势图和数据表不再显示哑铃伐木（6/22 那次的数据**保留**在 `SESSIONS` 和日历详情里，只是不进第一天的分组视图）。实现见 `HIDE_FROM_DAY` | 第一天 |
| 第四天前三动作固定次数 | **哑铃深蹲推举、俯身哑铃划船、哑铃摇摆 每轮固定 12 次**（摇摆因此从原15次改为12次） | 第四天 |

### 个别动作的进阶备注（用户口述，供助手给建议时参考）
- **哑铃箭步蹲**：17.5 lb each 已从"费劲"练到稳定，可考虑试 20 lb。
- **交替哑铃弯举**：20 lb each 仍偏吃力，用户要求继续用此重量多练几天，暂不加重。**8/9 再次确认：下次仍用 20 each × 每侧10 × 3。**注意 8/9 之后该动作已连续 3 次完全相同，下次若再相同就会满足规则 11.5 条件 2——但用户已明确要求维持，届时只陈述"已连续 4 次相同"这个事实，不建议加重。
- **臀桥**：用户曾定"三组统一22.5lb加片"，实际近期用到25lb加片。**8/16 起每组 10 次，今天实际 30 加片 × 10 × 3。**
- **深蹲**：用户偏好"第一组25lb，之后回22.5lb"这种首组冲重的模式。
- **坐姿哑铃推肩**：8/1 用 20/20/17.5；**8/14 三组全部 20 × 10 做满**（当初定的"等 20 稳到三组"已达成）。用户要求**下次仍维持 20 each × 10 × 3**。
- **绳索伐木**（8/1 定）：**进阶方式是加配重，不加次数**——次数固定每侧 8，想提升就直接上配重。用户明确否掉了"先加到每侧10次"的建议。下次仍为 20 × 每侧8 × 3。
- **高位下拉**：**8/14 起改为每组 10 次**，配重仍 85。原来"三组满 12 才上 90"的门槛随次数制改变已作废，新门槛待用户重新定。
- **哑铃深蹲**（8/4 定）：下次维持 **22.5 / 25 / 22.5**（重的一组放在**中间**，不再首组冲重）。注意这条覆盖了上面"首组冲重"的旧偏好。**8/16 起每组改 10 次**；今天实际 25 × 10 × 3。
- **罗马尼亚硬拉**（8/4 定）：下次维持 **32.5 / 30 / 30**。32.5 已有 7/14、8/4、8/16 三个数据点；8/16 与之前逐组相同，已连续 3 次相同。
- **哑铃伐木**（8/9 定）：下次仍用 **20 双手合拿 × 每侧10 × 3**。
- **哑铃平板卧推**（8/14 定）：下次维持 **25×12 / 27.5×10 / 27.5×10**；另按新规则每组改 10 次。
- **绳索伐木**：8/14 已连续 3 次完全相同（7/13、8/1、8/14），下次若再相同即满足规则 11.5 条件 2。用户此前定的进阶方式是**加配重、不加次数**。
- **第四天前 4 个动作**（8/11 定）：下次沿用今天的重量——深蹲推举 17.5 each、俯身划船 17.5 each、哑铃摇摆 20 双手合拿、简化波比跳自重，均 12/12/12（波比跳 8×3）。
- **简化波比跳**（8/11）：`8×3` 已连续 4 次完全相同，规则 11.5 条件 2 已于 8/11 触发并**提醒过一次**，用户选择维持。**不要再重复提醒**；该动作是自重，可动的杠杆是次数不是重量。
- **农夫行走**：8/4 第二组用到 37.5 each（此前一直是 35），为该动作新高；步数固定 60 步。**8/16 三组全部 37.5 each × 60 步，新高。**

---

## 5. 交付/更新工作流 (每次新训练)

1. 用户口头汇报 → 助手在**聊天里**用表格回显确认（这是主要记录方式）。
2. 措辞含糊处**先追问确认**（重量记法、日期、组次）。
3. 用户说"更新 artifact"后：
   - 在 `SESSIONS` 数组**末尾追加**新 session（保持日期升序）。
   - 更新 footer 里的日期区间上界（`数据区间 2026-05-30 → <新日期>`）。
   - **验证**：跑 `python3 tools.py validate`（本机无 node，校验器见第 10 节）。
4. **提交并推送（git push）发布**，然后在同一条回复里**同时**展示 artifact 与网址 —— 见**第 11 节规则 11.1 / 11.2**，这是每次改动都必须做的。

### 训练后点评风格（重要）
- **只给事实观察，不要鼓励/鞭策的话**（用户 6/27 明确要求去掉鞭策语）。
- **不要主动预测、也不要主动建议加重**（8/9 定，完整条件见**规则 11.5**）。事实观察照旧——新高、掉次数、容量变化、没合上的缺口都照说，但不要接"下次可以加到 X"这类建议。
- 数据点少时如实说明不确定性。

### 跨训练日动作的引用规则（8/1 定，用户纠正过一次）
**凡是出现在多个训练日的动作，报"上一次"时一律按跨训练日的时间轴最近一次，不按同一训练日的上一次。**

- 目前的跨训练日动作：**平板支撑**（第一天 + 第四天）。
- **哑铃伐木**在数据上仍是跨训练日（6/22 第一天做过一次），但 8/14 起已从第一天的视图里隐藏，实际只在第三天出现，引用时按第三天口径即可。
- 出错来源：数据表是按训练日分栏的，顺着"第一天"那一栏往回读会跳过中间第四天的记录。**趋势图本身是跨训练日汇总的，是对的。**
- 实例：8/1（第一天）练平板支撑，正确的"上一次"是 7/21 第四天的 36 秒，不是 7/13 第一天的 40 秒。
- 补充事实（给建议时用）：第四天的平板支撑接在无组间休的三轮循环之后，第一天的不是，所以两者秒数不完全可比——截至 8/14，第四天 30–41 秒、第一天 37–48 秒。

---

## 6. HTML Artifact 的功能规格

单文件 HTML（无框架、无外部 JS 依赖；仅用 Google Fonts）。三个视图用顶部 tab 切换：**趋势图 / 数据表 / 日历**。

### 6.1 首页（Header）
- 标题"训练历史与进阶追踪"，副标题精简为一句：**"自 2026 年 5 月 30 日起的每一次训练。"**
- **统计条 4 项**（顺序固定）：训练场次 · 跨越周期(天) · **平均每几日一练** · 训练日循环(固定4)。
  - "平均每几日一练" = 跨越天数 ÷ (场次 − 1)，保留1位小数。
  - **不显示累计容量**（曾有，已按用户要求移除）。

### 6.2 趋势图 (Graph)
- **按训练日划分，不按动作**。顶部按钮选第几训练日，该日**所有动作以小图并排（small multiples）显示在同一页**（`HIDE_FROM_DAY` 里登记的动作除外）。
- ⚠️ 注意：每张小图的数据来自 `seriesForExercise(name)`，是**跨训练日汇总**的。所以一个动作若同时出现在两个训练日，它在两个日的小图里画的是同一条完整曲线——这正是 8/14 把哑铃伐木从第一天移走的原因（那张图画的其实是第三天的历史）。
- 每个负重动作画**双线**：
  - **绿线 = 单组最大重量**（对应左侧可见 y 轴刻度）。
  - **蓝线（半透明）= 总容量 = Σ(每组重量 × 次数)**。**蓝线用独立的隐藏缩放**（因为容量数量级是几百、重量是几十，共用刻度会压扁其一）。左侧刻度只对绿线准确；蓝线只表达走势形状，不可从轴读数值。
  - ⚠️ 已知设计取舍：用户已被告知蓝线无可读刻度。若未来要改进，可选：给蓝线加右侧第二 y 轴 / 在蓝线点上直接标数值 / 依赖悬停提示（当前提示已含精确容量值）。
- **自重与计时动作**（自行车卷腹、简化波比跳、平板支撑）无重量，改用**总次数或总秒数**画单线。
- **未来预测**：对每个动作用**线性回归**外推，虚线向后预测 1 次训练；显示每周变化率和 **R²**（拟合优度）。预测点用空心点+虚线，有独立说明文字强调"纯数学外推、不考虑主观感受/疲劳/平台期、数据点少时不确定性大"。
- **数据点交互提示**：鼠标悬停（PC）或手指触摸（移动端）任一数据点，弹出该点详情（日期、各组重量×次数、最大重量、总容量；预测点则显示预测值+R²）。数据点有隐形放大热区便于触摸。

### 6.3 数据表 (Table)
- **按训练日分组**。每组内：**动作为行，历次训练(该训练日的每个日期)为列**，日期从左到右升序，便于同训练日横向对比（`HIDE_FROM_DAY` 里登记的动作不出现在该日的行里）。
- 顶部可切"全部"或单独某一训练日。
- 每个数据格显示该次各组"重量×次数"（多组换行），并有 **▲(升)/▼(降)/=(平)** 箭头标注相比上一次（按最大重量，自重动作按总次数）。
- 跳过的动作显示"跳过"（clay 色）；某训练日未出现的动作显示"—"。

### 6.4 日历 (Calendar)
- **紧凑布局**：每个月做小，**一行容纳 3–4 个月**，一年可在一页看完（当前用 CSS grid `auto-fill minmax(230px,1fr)`）。
- 练过的日子用该训练日的主题色填充，格内显示日号。部分完成的训练在格子右上角加一个小圆点标记。
- 悬停/点击某天弹出当天完整训练详情（含跳过项）。
- 底部有训练日颜色图例。

### 6.5 响应式 (PC + 移动端)
- 必须 PC 和移动端都可用。已有 viewport 标签。
- 断点：≤760px 趋势图单列；≤640px 手机布局（tab 平分、日历2列、提示框限宽 78vw、panel 内边距缩小）；≤380px 日历更紧凑。
- `@media (pointer: coarse)` 下加大数据点触摸热区和按钮点击区。

### 6.6 视觉风格（当前设计，可保留）
- 深色主题：背景 `#10130f`，主强调色酸橙绿 `--lime: #c6f24e`。
- 训练日配色：第一天 clay `#e08a5a`、第二天 sky `#6fc4d6`、第三天 violet `#b39ddb`、第四天 gold `#d8c24e`。
- 字体：标题 Fraunces(衬线)、正文 Archivo、数字/标签 Spline Sans Mono。
- 辅助色：容量蓝线用 `--sky`；预测虚线用 `--proj: #55604a`。

---

## 7. 关键实现细节（供改代码时参考）

- `SESSIONS` 数组 = 唯一数据源，按日期升序。
- `BODYWEIGHT_OR_TIME` Set 列出不计入重量趋势的动作（自行车卷腹、简化波比跳、平板支撑、平板支撑转体）。
- `HIDE_FROM_DAY` / `hiddenOnDay(day,name)`（8/14 加）：**按训练日隐藏动作**。只过滤趋势图和数据表里该训练日的动作清单，`SESSIONS` 数据和日历当天详情**不受影响**。当前内容：`{ 1: ["哑铃伐木"] }`。要再隐藏别的，只改这个对象即可。
- `volume(sets)` = Σ w×r（w=0 的组贡献0）。`maxWeight(sets)` = 各组最大 w。`repsTotal(sets)` = Σ 次数（自重动作趋势用）。
- `linreg(xs, ys)` 返回 `{slope, intercept, r2}`；x 轴单位是"距首次记录的天数"。
- `seriesForExercise(name)` 跨所有 session 收集某动作出现记录（跳过 skipped / 空 sets）。
- 趋势图每个动作小图：主线(绿)用 `syP` 映射到可见刻度，容量线(蓝)用独立 `syV` 映射；投影点按平均间隔天数外推 1 次。
- 数据点提示：每个点渲染一个透明大圆(`.pt-hit`)带 `data-pt` JSON，`wirePointTips()` 在每次重绘后绑定 hover+touch 事件。
- 共享提示框元素 `const tip` 在脚本靠前处声明（供图表点和日历格共用），避免 TDZ。
- 日历、表格、图表都随 `SESSIONS` 自动重算，新增 session 无需改视图逻辑——**只改数据数组 + footer 日期**即可。
- **每个训练日的动作显示顺序 = 该训练日第一场训练的记录顺序**（`exOrder` 按时间取首次出现）。后来调整过的顺序不会反映到趋势图和数据表里。
  - 已知偏差：第三天显示为 划船 → **绳索划船 → 弯举** → 伐木 → 卷腹（沿用 6/06 的老顺序），而实际练法是 划船 → **弯举 → 绳索划船** → 伐木 → 卷腹。
  - **8/14 用户明确表示按现状保留，不要改。** 不要再提议修这个，也不要"顺手"加显式顺序配置。

---

## 8. 完整训练历史（截至 2026-08-14，共 16 场）

以下是文件内 `SESSIONS` 的权威内容摘要（若与 HTML 内数组不一致，以 HTML 内数组为准）：

1. **2026-05-30 第二天**：深蹲 20each×12/12/12｜罗马尼亚硬拉 27.5/27.5/30 each×10｜箭步蹲 17.5/17.5/15 each×每侧8(17.5费劲)｜农夫行走 35each×60步 ×3｜臀桥 50/75/70(基座+片)×15
2. **2026-06-06 第三天**：单臂划船 17.5/22.5/22.5 each×每侧10｜绳索划船 85/80/75×12｜交替弯举 22.5(4)/20(10)/20(6) each(组3递减力竭)｜哑铃伐木 25双手合拿×每侧6/7/5｜自行车卷腹 每侧9/5/4(9档)
3. **2026-06-14 第四天**：深蹲推举 15each×12/11/9｜俯身划船 15/15/17.5 each×10/12/12｜哑铃摇摆 22.5/22.5/20双手合拿×15/8/12｜简化波比跳 8/8/8｜平板支撑 30/36/33秒
4. **2026-06-22 第一天**：卧推 22.5/25/25 each×12/12/9｜推肩 22.5(5)/17.5/17.5 each×10｜高位下拉 100(8)/85/85×12｜哑铃伐木 25(5)/17.5/17.5双手合拿×每侧8｜平板支撑 48/38/37秒
5. **2026-06-27 第二天**：深蹲 22.5each×12×3｜罗马尼亚硬拉 30each×10×3｜箭步蹲 17.5each×每侧8×3｜农夫行走 35each×60步×3｜臀桥 25片×10/15/15
6. **2026-07-05 第三天**：单臂划船 22.5each×每侧10×3｜交替弯举 20each×每侧10×3(仍吃力)｜绳索划船 80×12×3｜哑铃伐木 25/17.5/20双手合拿×每侧10｜自行车卷腹 每侧10/7/7(9档)
7. **2026-07-07 第四天**：深蹲推举 15each×12×3｜俯身划船 17.5each×12/10/10｜哑铃摇摆 20双手合拿×15×3｜简化波比跳 8×3｜平板支撑 35秒×3
8. **2026-07-13 第一天**：卧推 25each×12/10/12｜推肩 17.5each×10×3｜高位下拉 85×12/12/9｜绳索伐木 20×每侧8×3(首次基准)｜平板支撑 40秒×3
9. **2026-07-14 第二天(部分)**：深蹲 25/22.5/22.5 each×12｜罗马尼亚硬拉 32.5/30/30 each×10｜箭步蹲 17.5each×每侧8×2(第三组跳过)｜农夫行走(跳过)｜臀桥 25片×15×1(其余跳过)
10. **2026-07-19 第三天**：单臂划船 25/22.5/22.5 each×每侧10｜交替弯举 20each×每侧10×3｜绳索划船 85/80/80×12｜哑铃伐木 20双手合拿×每侧10×3｜自行车卷腹 每侧8×3(9档)
11. **2026-07-21 第四天**：深蹲推举 17.5each×12/12/8｜俯身划船 17.5each×12×3｜哑铃摇摆 20双手合拿×12×3｜简化波比跳 8×3｜平板支撑 36秒×3
12. **2026-08-01 第一天**：卧推 25/25/27.5 each×12(27.5为该动作新高)｜推肩 20/20/17.5 each×10(20首次做满10次)｜高位下拉 85×12/12/11｜绳索伐木 20×每侧8×3(同7/13)｜平板支撑 40秒×3
13. **2026-08-04 第二天**：深蹲 22.5/25/22.5 each×12(重组放中间)｜罗马尼亚硬拉 32.5/30/30 each×10(同7/14)｜箭步蹲 17.5each×每侧8×3(自6/27以来首次做满三组)｜农夫行走 35/37.5/35 each×60步(37.5为新高)｜臀桥 25片×15×3
14. **2026-08-09 第三天**：单臂划船 25/25/22.5 each×每侧10(25延到2组)｜交替弯举 20each×每侧10×3(连续第3次相同)｜绳索划船 85/80/80×12(同7/19)｜哑铃伐木 20双手合拿×每侧10×3(同7/19)｜自行车卷腹 每侧8×3(9档)
15. **2026-08-11 第四天**：深蹲推举 17.5each×12×3(17.5首次三轮全满)｜俯身划船 17.5each×12×3(同7/21)｜哑铃摇摆 20双手合拿×12×3(同7/21)｜简化波比跳 8×3(连续第4次相同)｜平板支撑 41秒×3(总123秒，与6/22并列最高)
16. **2026-08-14 第一天**：卧推 25×12/27.5×10/27.5×10 each(27.5延到2组)｜推肩 20each×10×3(20首次三组做满)｜高位下拉 85×10×3(改10次制)｜绳索伐木 20×每侧8×3(连续第3次相同)｜平板支撑 42秒×3(总126秒，历史最高)

---

## 9. 交给 Claude Code 后的注意事项

- 数据的**唯一真源是 HTML 内的 `SESSIONS` 数组**；本 md 第8节是人类可读摘要，冲突时以 HTML 为准。
- 继续维护时保持**单文件、无 localStorage、无外部依赖**的约束（这是 artifact 环境要求，也便于用户单文件下载）。
- 新增训练：只需向 `SESSIONS` 追加 + 更新 footer 日期 + 验证渲染。
- 任何影响"计划结构/记录约定/动作替换/点评风格"的新决定，都应回写更新到本说明书，保持它是完整的单一交接来源。

---

## 10. 工作区与发布管线（2026-07-30 建立，2026-08-16 迁到 GitHub Pages）

### 10.1 文件夹结构（保持干净，不要新增散落脚本）

| 文件 | 作用 |
|---|---|
| `workout-history.html` | **唯一真源**，所有修改都改这个文件 |
| `tools.py` | 校验器 + 发布构建，**合并在一个文件里** |
| `WorkoutTracker_Rules.md` | 本说明书 |
| `CLAUDE.md` | **新会话的入口**：指向本文件，并摘出第 11 节的强制规则 |
| `AGENTS.md` / `README.md` | 给非 Claude harness 的同一入口，内容指向 `CLAUDE.md` |
| `.build/` | **全部生成物**（发布用文件、字体缓存、`publish.json`、`preview.html`） |

`.build/` 里的东西是**一次性产物**：绝不手改、绝不当作第二真源。要改线上页面就改源文件再重新构建。

### 10.1.1 线上托管（2026-08-16 迁入 GitHub）

- 仓库：`github.com/huixin96f/Workout-Tracker`（**公开**，free 计划不支持私有仓库 Pages）
- 线上网址（**新书签**）：https://huixin96f.github.io/Workout-Tracker/workout-history.html
- 旧 Claude artifact 网址（**已弃用**，账号弃用即失效）：https://claude.ai/code/artifact/a4d428a8-4f06-4fbd-b844-4cb0c7760ea1
- **发布方式 = `git push`**：改完源文件 → `validate` → commit → push，GitHub Actions 自动部署（`.nojekyll` 已加，页面是源文件直出，非 `.build/` 构建产物）。
- 认证：凭据存在 macOS 钥匙串（username=oauth2），push 不需要输密码。

### 10.2 命令（只依赖 python3 标准库）

```
python3 tools.py validate   # 数据校验 + 真实运行一遍 JS
python3 tools.py build      # 校验通过后生成 .build/workout-history.artifact.html（仅供本地预览/留档，不再发布）
python3 tools.py preview    # 本地起服务，用来在手机宽度下实测
python3 tools.py fonts      # 重新抓取内嵌字体（一次性，已缓存就不用再跑）
python3 tools.py streaks    # 规则 11.5 的判定：逐个动作算连续完全相同的次数
git add -A && git commit -m "..." && git push   # 发布 = 推送到 GitHub，自动部署到 Pages
```

JS 的运行时检查配一个 DOM stub，把四个训练日的图表、五种表格筛选、日历、以及**每一个数据点的
提示框**都真跑一遍。引擎按顺序自动挑选（8/15 加，此前写死 macOS）：

1. `node`（若 PATH 里有）
2. macOS 自带的 JavaScriptCore：`osascript -l JavaScript`
3. **两个都没有** → 打印醒目警告、**跳过渲染检查**，数据校验照常跑完并如实说明。此前这种情况会直接抛 `FileNotFoundError` 崩掉，连带 build 和发布全断。

两种引擎已实测给出完全一致的结果（107 个图表点 / 16 个日历格）。

### 10.3 校验器会拦下的事（任何一条不过就**不发布**）

1. `SESSIONS` 解析不了 / 日期不是严格升序 / 日期重复 / `day` 不在 1–4。
2. `sets` 不是 `[重量, 次数]` 成对、重量为负、次数 ≤ 0。
3. `skipped` 与空 `sets` 不一致（跳过必须 `sets: []`，空 `sets` 必须标 `skipped`）。
4. 同一场训练里动作重名；`unit` 缺失（陌生 unit 只警告不拦）。
5. 动作全程无重量却没登记进 `BODYWEIGHT_OR_TIME`（否则趋势图会是空的），或反之。
6. **日历 `const months=[...]` 没覆盖到的月份** —— 日历只渲染这个硬编码数组里的月份，漏掉的训练不会报错、只会静默消失。校验器会拦。（8/1 已因此把 8 月加进去，当前是 `[5,6,7,8]`；**跨到 9 月时要记得再加**。）
7. footer 的日期区间上下界、header 副标题的起始日期，与 `SESSIONS` 首末场对不上。
8. JS 运行时报错、图表点渲染为空、提示框 JSON 坏掉、统计条不是 4 项。

### 10.4 发布身份（**URL 已被收藏，不能变**）

**线上网址（唯一，已收藏）：** https://huixin96f.github.io/Workout-Tracker/workout-history.html
仓库名 / 路径 / 文件名任何一个变了都会换网址。发布流程就是 `git push` 到 main 分支，GitHub Pages 自动部署。

### 10.5 构建时已验证过的坑（别重新踩；以下坑针对旧的 Claude artifact 发布，现仅作历史参考）

- 发布时页面会被塞进 host 自己的文档骨架里，所以**不能直接发布源文件**；构建产物只包含"该在 body 里的东西"。
  - 现 GitHub Pages 直接发**源文件** `workout-history.html`，无此问题。
- **viewport meta 会丢** → 产物里用一段引导脚本在运行时补回 `width=device-width`，否则手机上按桌面宽度排版。
- **charset meta 也会丢，且写在 body 里已经太晚** → 产物必须是**纯 ASCII**：markup 用 `&#NNNN;`，`<script>` 里用 `\uXXXX`，`<style>` 里的非 ASCII 只允许出现在注释中（音译处理），出现在真实取值里就直接构建失败。已实测：即使浏览器把页面当 windows-1252 解码，中文照样正常显示。
- **artifact 环境的 CSP 屏蔽一切外部请求**，Google Fonts 的 `@import` 永远加载不了 → 构建时把 Fraunces / Archivo / Archivo Narrow / Spline Sans Mono 的 **ASCII 子集**内嵌成 data URI（中文本来就走系统字体回退），并把失效的 `@import` 删掉。
- 源文件里 `body{}` 和 `*{}` 这类规则会污染宿主页面 → 构建时把**整份样式表重新挂到 `#wt-root` 包裹层**上；找不到该改的规则就直接构建失败。
- `@import` 的 URL 里**本身带分号**（字重列表 `500;600`），按分号切会切坏它。
- 发布页面是**只读的查看器**：拖拽、点击这些交互还能用，但刷新后什么都不会保存。数据只通过聊天改。

---

## 11. 回复里必须包含什么（**最重要的一节**）

规则 11.1–11.3 由**改动**触发；规则 11.4 由**提问**触发，不需要有任何改动。

### 规则 11.1 —— 两样都要给，缺一不可

**只要 artifact 发生任何改动，回复里必须同时包含这两样：**

| # | 内容 | 方式 |
|---|---|---|
| 1 | **artifact 本身**，在聊天里内联渲染出来 | `SendUserFile`，`display: "render"` |
| 2 | **线上网址**，可点击 | 写成链接：https://huixin96f.github.io/Workout-Tracker/workout-history.html |

"任何改动"= 新增/编辑/完成/重排/删除数据，以及任何结构、CSS、JS 的修改。
**无论改动多小都要两样齐全；只给其中一样不是省事，是漏做。**

### 规则 11.2 —— 每次改动都要提交并推送发布

在**同一条回复**里完成，这样手机上那份永远不会在两人都没察觉的情况下变旧。

### 规则 11.3 —— 顺序

改 `workout-history.html` → `python3 tools.py validate` → `git commit` + `git push`
→ 回复里按规则 11.1 展示两样。

校验不通过就**不构建、不发布**，先把问题说清楚。

### 规则 11.4（8/9 定，当天扩充）—— 问"今天练什么"时要给的三样

**只要用户问今天该做哪些动作 / 今天练什么 / 下次练什么，回复里必须包含这三样：**

| # | 内容 | 方式 |
|---|---|---|
| 1 | **上次做的重量和组数**（该训练日每个动作） | 表格 |
| 2 | **artifact 本身**，内联渲染 | `SendUserFile`，`display: "render"` |
| 3 | **线上网址**，可点击 | https://huixin96f.github.io/Workout-Tracker/workout-history.html |

- 触发条件是**提问**，不是改动。这类回复通常什么都没改，照样三样齐全。
- 用的是**上一次已发布的版本**，不需要为了回答问题重新构建或重新发布（除非同时确实改了数据，那才走 11.1/11.2）。
- 引用"上次"时遵守第 5 节的**跨训练日规则**，并且跳过的动作要往前找到**最近一次真正做过**的那次。
- 用意：用户常在去健身房的路上或在器械前用手机看，计划、历史、artifact 要在同一条消息里够得着。

### 规则 11.5（8/9 定）—— 不主动建议加重，只在两个条件下提醒

**默认不做任何加重预测和加重建议。** 只在下面**两个条件之一**成立时，才提醒该适当加重：

1. **用户曾要求就某个动作提醒**（例如箭步蹲的 20 lb 测试就是用户自己挂起来的）；或
2. **同一动作用完全相同的重量和组数超过 3 次训练**——即第 4 次及以后仍然一模一样时才提醒。

判定用 `python3 tools.py streaks`，不要靠印象。它按 `SESSIONS` 逐个动作算当前连续相同（重量×次数完全一致）的次数，`>3` 的会被标出来。

- "相同"= 每一组的重量和次数都逐组一致，组数也一致。任何一组变了，计数归零重新算。
- 达标时只说"这个动作已经连续 N 次完全相同"，把加多少留给用户决定。
- 事实观察不受这条限制：新高、掉次数、没做满的组、容量变化，照说。
- artifact 里的线性回归预测线是**图上的功能**，保留不动；这条规则约束的是**聊天里的建议**。

#### 11.5.1 提醒的生命周期（8/9 定）—— 提醒完就忘掉

条件 1（用户挂起的待提醒项）**默认只提醒一次**，提醒过就从待提醒清单里划掉，不再重复。

- 用户**指定了次数**时按指定次数来。例："接下来的两次都用相同重量" → 在**接下来的 2 次训练**里各提醒一次，第 3 次起不再提。
- **"同天的训练"= 同一个训练日编号**（第几天），不是日历天。第二天的动作就在接下来的第 2、3 次"第二天"训练里提醒，中间的第三天/第四天训练不算。
- 提醒次数用完即失效，不需要用户再说一次"别提了"。

**当前挂起的待提醒项：**

| 动作 | 内容 | 剩余次数 | 何时提 |
|---|---|---|---|
| （空）| — | — | — |

**已用完的提醒**（记录在案，不要再提）：

| 动作 | 内容 | 提醒于 |
|---|---|---|
| 哑铃箭步蹲 | 可以试 20 lb each（17.5 已稳定） | 2026-08-16 第二天，剩余次数已归零 |

### 规则 11.6（8/9 定）—— 训练中每记录一个动作，要报下一个 + 全部未做的

用户在训练途中逐个汇报动作时，**每记录完一个动作，回复里必须包含：**

1. **下一个动作是什么**；
2. **今天所有还没做的动作，连同各自的重量和组数**——不只是下一个；
3. **artifact 的链接**：https://huixin96f.github.io/Workout-Tracker/workout-history.html

这样用户在器械之间走动时，一条消息里就能看到剩下全部的目标数字和历史，不用往回翻。

给**链接**即可，不必内联渲染 artifact（训练中不改数据，也不重新发布；内联渲染只在规则 11.1 的改动场景和规则 11.4 的计划提问场景要求）。

- 引用的数字遵守第 5 节的跨训练日规则，跳过的动作要往前找到**最近一次真正做过**的那次。
- 只给数字，不给加重建议（规则 11.5）。
- 最后一个动作记录完之后，这条自然失效——改走"今日训练结束"的更新流程（规则 11.1–11.3）。
